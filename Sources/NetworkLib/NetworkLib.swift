// Minimal stub for the NetworkLib target.
// This file is required by SPM — a target with no sources is rejected.
import NIO
import Logging

public enum NetworkLib {
    static let logger = Logger(label: "com.probe.NetworkLib")
}
